<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid " style="background-color: rgb(252, 244, 236);">
          <a class="navbar-brand" href="#">AERO <i class="fa-solid fa-plane"></i></a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav me-auto">
            <div class="d-flex gap-5">
              <a class="nav-link" aria-current="page" href="/">Home</a>
              <a class="nav-link" href="/flights">Explore Flights</a>
              <a class="nav-link" href="/guidlines">Guidlines</a>
              <a class="nav-link" href="/about">About</a>

              <% if (curruser && curruser.type === "admin") { %>
                <a class="nav-link" href="/addFlight">Add Flight</a>
                <a class="nav-link" href="/staff">Add Staff</a>
              <% } %>
              
              <% if (curruser && curruser.type === "user") { %>
                <a class="nav-link" href="/mybookings">Mybookings</a>
              <% } %>
              
            </div>
            </div>
            <div class="navbar-nav ms-auto">
                <div class="d-flex gap-3">
                  <% if(!curruser) {%>
                    <a class="nav-link" aria-current="page" href="/login">login</a>
                    <a class="nav-link" href="/signup">signup</a>
                    <% } %>
                    <% if(curruser) {%>
                    <a class="nav-link" href="/logout">logout</a>
                    <% } %>
            </div>
          </div>
          </div>
        </div>
      </nav>
</body>
</html>